# Nixpacks configuration
# We'll work with Caddy if it's detected, but ensure Node.js starts first

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x", "curl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "./start-server.sh"

