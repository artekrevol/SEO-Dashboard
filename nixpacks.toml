# Nixpacks configuration to prevent Caddy auto-detection
# and use standard Node.js startup

[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[phases.start]
cmd = "npm run start"

